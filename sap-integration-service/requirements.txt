# Firebase Admin SDK (incluye google-cloud-firestore y grpcio)
firebase-admin==6.5.0

# Cliente HTTP para consumir la API OData de SAP
httpx==0.27.0

# Parseo XML robusto (la API OData puede responder en XML plano / Atom)
lxml==5.2.2

# Carga de variables de entorno desde .env
python-dotenv==1.0.1
